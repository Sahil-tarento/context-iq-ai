# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib
contextiq
contextiq-cli
contextiq-mcp
bin/
plugins/vscode/node_modules

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Dependency directories
vendor/

# Go workspace file
go.work
go.work.sum

# IDEs and editors
.idea/
# VS Code — ignore user-specific settings but track mcp.json for team
.vscode/*
!.vscode/mcp.json
# Cursor — ignore user-specific settings but track mcp.json for team
.cursor/*
!.cursor/mcp.json
*.swp
*.swo
*~

# OS specific
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
